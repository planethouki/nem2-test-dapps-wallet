import ModelType from '../ModelType'
import ModelBase from '../ModelBase'

export default class BackgroundBeforeSetUpInfo extends ModelBase {
  constructor (id) {
    super(ModelType.BACKGROUND_BEFORE_SETUP, id)
  }
}
