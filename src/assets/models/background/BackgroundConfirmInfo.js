import ModelType from '../ModelType'
import ModelBase from '../ModelBase'

export default class BackgroundConfirmInfo extends ModelBase {
  constructor (id) {
    super(ModelType.BACKGROUND_CONFIRM, id)
  }
}
